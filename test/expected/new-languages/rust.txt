









pub fn add(a: i32, b: i32) -> i32 {
    
    let url = "https://example.com/*not-a-comment*/";
    let pattern = "/*still*/ not a comment";
    a + b }

pub struct Point {
    pub x: f64,  
    pub y: f64,  
}
